type Mutation {
  createAccount(
    username: String!
    email: String!
    firstName: String
    lastName: String
    bio: String
  ): Boolean!
}

# This Query is Fake
type Query {
  something: String!
}
